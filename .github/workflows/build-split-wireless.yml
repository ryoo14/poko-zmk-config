name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # west.yml で固定した zmk と同じ commit に揃える。
    # @main のままだと Merge Output Artifacts の失敗など上流の変更をそのまま踏む。
    uses: cormoran/zmk/.github/workflows/build-user-config.yml@e5c9b6915b56801193e359dd9bad4a167ce0d1b8
    with:
      archive_name: poko-split-wireless
