include:
  # 左 = ペリフェラル（キーのみ）
  - board: xiao_ble
    shield: rhyn47_left
  # 右 = セントラル（キー + トラックボール）
  - board: xiao_ble
    shield: rhyn47_right
  # ペアリングが壊れた時の設定リセット用（書いたあと必ず本ファームを焼き直す）
  - board: xiao_ble
    shield: settings_reset
