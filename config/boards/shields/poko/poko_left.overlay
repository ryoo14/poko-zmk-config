/*
 * poko 左手（ペリフェラル・トラックボールなし）
 *
 * col-offset は既定の 0 のままなので、物理列0-5 = 論理列0-5。
 * 左メイン基板にはセンサーが無いので SPI/PMW3610 は一切書かない
 *   → クロックも通信も出ず、無駄な電力を食わない（設計書 §12.7）。
 *
 * kscan は poko.dtsi の include より前に置く（poko.dtsi の chosen が &kscan0 を参照するため）。
 */

#include <zephyr/dt-bindings/gpio/gpio.h>

/ {
    /*
     * マトリクス 4行 × 6列（24キー）
     *
     * ピン割当は本番基板 left.kicad_pcb の netlist から採取（U1 = XIAO nRF52840 Plus）。
     * 右基板とは割当が違うので、共通の poko.dtsi ではなくここに書いている。
     * D7 (P1.12) は未接続。
     *
     * ダイオード向き: SW.1→COL / SW.2→ダイオードのアノード / カソード→ROW。
     * つまり COL→ROW に導通するので col2row。COL が出力、ROW が入力（プルダウン）。
     * ※ 試作基板は逆向き（row2col）だった。v0.1-proto タグ参照。
     */
    kscan0: kscan {
        compatible = "zmk,kscan-gpio-matrix";
        diode-direction = "row2col";
        wakeup-source;

        /* col2row なので row = 入力（プルダウン） */
        row-gpios
            /*
            = <&gpio0  3 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW0 = D1  / P0.03
            , <&gpio0  4 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW1 = D4  / P0.04
            , <&gpio0  5 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW2 = D5  / P0.05
            , <&gpio1 11 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW3 = D6  / P1.11
            */
            = <&gpio1 11 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW0 = D1  / P0.03
            , <&gpio0  5 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW1 = D4  / P0.04
            , <&gpio0  4 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW2 = D5  / P0.05
            , <&gpio0 29 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   // ROW3 = D6  / P1.11
            ;

        /* col = 出力 */
        col-gpios
            /*
            = <&gpio0  2 GPIO_ACTIVE_HIGH>   // COL0 = D0  / P0.02
            , <&gpio0 28 GPIO_ACTIVE_HIGH>   // COL1 = D2  / P0.28
            , <&gpio0 29 GPIO_ACTIVE_HIGH>   // COL2 = D3  / P0.29
            , <&gpio1 13 GPIO_ACTIVE_HIGH>   // COL3 = D8  / P1.13
            , <&gpio1 14 GPIO_ACTIVE_HIGH>   // COL4 = D9  / P1.14
            , <&gpio1 15 GPIO_ACTIVE_HIGH>   // COL5 = D10 / P1.15
            */
            = <&gpio0 28 GPIO_ACTIVE_HIGH>   // COL0 = D0  / P0.02
            , <&gpio0  3 GPIO_ACTIVE_HIGH>   // COL1 = D2  / P0.28
            , <&gpio1 12 GPIO_ACTIVE_HIGH>   // COL2 = D3  / P0.29
            , <&gpio1 13 GPIO_ACTIVE_HIGH>   // COL3 = D8  / P1.13
            , <&gpio1 14 GPIO_ACTIVE_HIGH>   // COL4 = D9  / P1.14
            , <&gpio1 15 GPIO_ACTIVE_HIGH>   // COL5 = D10 / P1.15
            ;
    };
};

#include "poko.dtsi"
